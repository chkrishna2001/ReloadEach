# Release Notes

## Unreleased
- Added `Reload All Projects` from the Tools menu, solution context menu, and toolbar placement.
- Added `Reload Selected Projects` for one or more selected projects in Solution Explorer.
- Added `Cancel ReloadEach` for stopping an active reload run.
- Added configurable `Delay seconds` option under `Tools > Options > ReloadEach > General`.
- Fixed VSIX packaging, command table resource generation, package loading, and official VSSDK build output.
- Added GitHub Actions build workflow and VSIX artifact upload.
